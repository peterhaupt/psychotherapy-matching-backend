name: curavani_backend_prod
services:
  # PostgreSQL Database
  postgres-prod:
    image: postgres:16-alpine
    container_name: postgres-prod
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_EXTERNAL_PORT}:${DB_PORT}"
    volumes:
      - postgres_prod_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - curavani_backend_prod

  # PostgreSQL Backup Service for Production
  postgres-backup-prod:
    build:
      context: .
      dockerfile: docker/postgres-backup/Dockerfile
    container_name: postgres-backup-prod
    environment:
      # Backup Configuration
      BACKUP_ENV: prod
      # Database Connection
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_HOST: postgres-prod
      DB_PORT: ${DB_PORT}
    volumes:
      - ./backups:/backups
    depends_on:
      postgres-prod:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - curavani_backend_prod

  # PgBouncer Connection Pooler
  pgbouncer-prod:
    image: bitnami/pgbouncer:latest
    container_name: pgbouncer-prod
    environment:
      # Database connection settings
      POSTGRESQL_HOST: postgres-prod
      POSTGRESQL_PORT: ${DB_PORT}
      POSTGRESQL_DATABASE: ${DB_NAME}
      POSTGRESQL_USERNAME: ${DB_USER}
      POSTGRESQL_PASSWORD: ${DB_PASSWORD}
      # PgBouncer specific settings
      PGBOUNCER_DATABASE: ${DB_NAME}
      PGBOUNCER_PORT: ${PGBOUNCER_PORT}
      PGBOUNCER_BIND_ADDRESS: 0.0.0.0
      PGBOUNCER_AUTH_TYPE: md5
      PGBOUNCER_AUTH_USER: ${DB_USER}
      PGBOUNCER_AUTH_QUERY: "SELECT usename, passwd FROM pg_shadow WHERE usename=$1"
      PGBOUNCER_POOL_MODE: session
      PGBOUNCER_MAX_CLIENT_CONN: 1000
      PGBOUNCER_DEFAULT_POOL_SIZE: 25
      PGBOUNCER_IGNORE_STARTUP_PARAMETERS: "extra_float_digits"
      # Create the databases.ini and users.ini files
      PGBOUNCER_DSN_${DB_NAME}: "host=postgres-prod port=${DB_PORT} dbname=${DB_NAME}"
      # Set userlist
      PGBOUNCER_USERLIST: "${DB_USER}:${DB_PASSWORD}"
      # Admin user
      PGBOUNCER_ADMIN_USERS: ${DB_USER}
      # Stats user
      PGBOUNCER_STATS_USERS: ${DB_USER}
      # Extra settings for better compatibility
      PGBOUNCER_EXTRA_FLAGS: "--verbose"
    ports:
      - "${PGBOUNCER_EXTERNAL_PORT}:${PGBOUNCER_PORT}"
    depends_on:
      postgres-prod:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "sh", "-c", "PGPASSWORD=${DB_PASSWORD} /opt/bitnami/postgresql/bin/psql -h localhost -p ${PGBOUNCER_PORT} -U ${DB_USER} -d ${DB_NAME} -c 'SELECT 1'"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - curavani_backend_prod

  # Kafka Message Broker
  zookeeper-prod:
    image: confluentinc/cp-zookeeper:latest
    container_name: zookeeper-prod
    environment:
      ZOOKEEPER_CLIENT_PORT: ${ZOOKEEPER_CLIENT_PORT}
      ZOOKEEPER_TICK_TIME: ${ZOOKEEPER_TICK_TIME}
    ports:
      - "${ZOOKEEPER_EXTERNAL_PORT}:${ZOOKEEPER_CLIENT_PORT}"
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "${ZOOKEEPER_CLIENT_PORT}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - curavani_backend_prod

  kafka-prod:
    image: confluentinc/cp-kafka:latest
    container_name: kafka-prod
    depends_on:
      zookeeper-prod:
        condition: service_healthy
    environment:
      KAFKA_BROKER_ID: ${KAFKA_BROKER_ID}
      KAFKA_ZOOKEEPER_CONNECT: ${KAFKA_ZOOKEEPER_CONNECT}
      KAFKA_ADVERTISED_LISTENERS: ${KAFKA_ADVERTISED_LISTENERS}
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: ${KAFKA_LISTENER_SECURITY_PROTOCOL_MAP}
      KAFKA_LISTENERS: ${KAFKA_LISTENERS}
      KAFKA_INTER_BROKER_LISTENER_NAME: ${KAFKA_INTER_BROKER_LISTENER_NAME}
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: ${KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR}
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: '${KAFKA_AUTO_CREATE_TOPICS_ENABLE}'
    healthcheck:
      test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:9092"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - curavani_backend_prod

  # Patient Service
  patient_service-prod:
    build:
      context: .
      dockerfile: patient_service/Dockerfile
    container_name: patient_service-prod
    environment:
      # Core Database Configuration
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_HOST: postgres-prod
      DB_PORT: ${DB_PORT}
      # PgBouncer Configuration
      PGBOUNCER_HOST: pgbouncer-prod
      PGBOUNCER_PORT: ${PGBOUNCER_PORT}
      # Kafka Configuration
      KAFKA_BOOTSTRAP_SERVERS: ${KAFKA_BOOTSTRAP_SERVERS}
      KAFKA_LOG_LEVEL: ${KAFKA_LOG_LEVEL}
      # Flask Configuration
      FLASK_ENV: ${FLASK_ENV}
      FLASK_DEBUG: ${FLASK_DEBUG}
      LOG_LEVEL: ${LOG_LEVEL}
      # Security Configuration
      SECRET_KEY: ${SECRET_KEY}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      # Service Configuration
      SERVICE_ENV_SUFFIX: ${SERVICE_ENV_SUFFIX}
      SERVICE_HOST: ${SERVICE_HOST}
      # System Configuration
      SYSTEM_NOTIFICATION_EMAIL: ${SYSTEM_NOTIFICATION_EMAIL}
      # Service Ports (all services need to know all ports for inter-service communication)
      PATIENT_SERVICE_PORT: ${PATIENT_SERVICE_PORT}
      THERAPIST_SERVICE_PORT: ${THERAPIST_SERVICE_PORT}
      MATCHING_SERVICE_PORT: ${MATCHING_SERVICE_PORT}
      COMMUNICATION_SERVICE_PORT: ${COMMUNICATION_SERVICE_PORT}
      GEOCODING_SERVICE_PORT: ${GEOCODING_SERVICE_PORT}
      SCRAPING_SERVICE_PORT: ${SCRAPING_SERVICE_PORT}
      # Internal Service Ports (for container-to-container communication)
      PATIENT_SERVICE_INTERNAL_PORT: ${PATIENT_SERVICE_INTERNAL_PORT}
      THERAPIST_SERVICE_INTERNAL_PORT: ${THERAPIST_SERVICE_INTERNAL_PORT}
      MATCHING_SERVICE_INTERNAL_PORT: ${MATCHING_SERVICE_INTERNAL_PORT}
      COMMUNICATION_SERVICE_INTERNAL_PORT: ${COMMUNICATION_SERVICE_INTERNAL_PORT}
      GEOCODING_SERVICE_INTERNAL_PORT: ${GEOCODING_SERVICE_INTERNAL_PORT}
      # GCS Configuration
      GCS_READER_CREDENTIALS_PATH: ${GCS_READER_CREDENTIALS_PATH}
      GCS_DELETER_CREDENTIALS_PATH: ${GCS_DELETER_CREDENTIALS_PATH}
      GCS_IMPORT_BUCKET: ${GCS_IMPORT_BUCKET}
      PATIENT_IMPORT_CHECK_INTERVAL_SECONDS: ${PATIENT_IMPORT_CHECK_INTERVAL_SECONDS}
      PATIENT_IMPORT_LOCAL_PATH: ${PATIENT_IMPORT_LOCAL_PATH}
    ports:
      - "${PATIENT_SERVICE_PORT}:${PATIENT_SERVICE_INTERNAL_PORT}"
    volumes:
      - ./credentials:/credentials:ro
      - ./data/patient_imports:/data/patient_imports
    depends_on:
      pgbouncer-prod:
        condition: service_healthy
      kafka-prod:
        condition: service_healthy
    command: python app.py
    networks:
      - curavani_backend_prod

  # Therapist Service
  therapist_service-prod:
    build:
      context: .
      dockerfile: therapist_service/Dockerfile
    container_name: therapist_service-prod
    environment:
      # Core Database Configuration
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_HOST: postgres-prod
      DB_PORT: ${DB_PORT}
      # PgBouncer Configuration
      PGBOUNCER_HOST: pgbouncer-prod
      PGBOUNCER_PORT: ${PGBOUNCER_PORT}
      # Kafka Configuration
      KAFKA_BOOTSTRAP_SERVERS: ${KAFKA_BOOTSTRAP_SERVERS}
      KAFKA_LOG_LEVEL: ${KAFKA_LOG_LEVEL}
      # Flask Configuration
      FLASK_ENV: ${FLASK_ENV}
      FLASK_DEBUG: ${FLASK_DEBUG}
      LOG_LEVEL: ${LOG_LEVEL}
      # Security Configuration
      SECRET_KEY: ${SECRET_KEY}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      # Service Configuration
      SERVICE_ENV_SUFFIX: ${SERVICE_ENV_SUFFIX}
      SERVICE_HOST: ${SERVICE_HOST}
      # System Configuration
      SYSTEM_NOTIFICATION_EMAIL: ${SYSTEM_NOTIFICATION_EMAIL}
      # Service Ports (all services need to know all ports for inter-service communication)
      PATIENT_SERVICE_PORT: ${PATIENT_SERVICE_PORT}
      THERAPIST_SERVICE_PORT: ${THERAPIST_SERVICE_PORT}
      MATCHING_SERVICE_PORT: ${MATCHING_SERVICE_PORT}
      COMMUNICATION_SERVICE_PORT: ${COMMUNICATION_SERVICE_PORT}
      GEOCODING_SERVICE_PORT: ${GEOCODING_SERVICE_PORT}
      SCRAPING_SERVICE_PORT: ${SCRAPING_SERVICE_PORT}
      # Internal Service Ports (for container-to-container communication)
      PATIENT_SERVICE_INTERNAL_PORT: ${PATIENT_SERVICE_INTERNAL_PORT}
      THERAPIST_SERVICE_INTERNAL_PORT: ${THERAPIST_SERVICE_INTERNAL_PORT}
      MATCHING_SERVICE_INTERNAL_PORT: ${MATCHING_SERVICE_INTERNAL_PORT}
      COMMUNICATION_SERVICE_INTERNAL_PORT: ${COMMUNICATION_SERVICE_INTERNAL_PORT}
      GEOCODING_SERVICE_INTERNAL_PORT: ${GEOCODING_SERVICE_INTERNAL_PORT}
      # Import Configuration
      THERAPIST_IMPORT_FOLDER_PATH: ${THERAPIST_IMPORT_FOLDER_PATH}
      THERAPIST_IMPORT_CHECK_INTERVAL_SECONDS: ${THERAPIST_IMPORT_CHECK_INTERVAL_SECONDS}
      THERAPIST_IMPORT_ENABLED: ${THERAPIST_IMPORT_ENABLED}
    ports:
      - "${THERAPIST_SERVICE_PORT}:${THERAPIST_SERVICE_INTERNAL_PORT}"
    volumes:
      - ../curavani_scraping/data/processed:/scraping_data:ro
    depends_on:
      pgbouncer-prod:
        condition: service_healthy
      kafka-prod:
        condition: service_healthy
    command: python app.py
    networks:
      - curavani_backend_prod

  # Matching Service
  matching_service-prod:
    build:
      context: .
      dockerfile: matching_service/Dockerfile
    container_name: matching_service-prod
    environment:
      # Core Database Configuration
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_HOST: postgres-prod
      DB_PORT: ${DB_PORT}
      # PgBouncer Configuration
      PGBOUNCER_HOST: pgbouncer-prod
      PGBOUNCER_PORT: ${PGBOUNCER_PORT}
      # Kafka Configuration
      KAFKA_BOOTSTRAP_SERVERS: ${KAFKA_BOOTSTRAP_SERVERS}
      KAFKA_LOG_LEVEL: ${KAFKA_LOG_LEVEL}
      # Flask Configuration
      FLASK_ENV: ${FLASK_ENV}
      FLASK_DEBUG: ${FLASK_DEBUG}
      LOG_LEVEL: ${LOG_LEVEL}
      # Security Configuration
      SECRET_KEY: ${SECRET_KEY}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      # Service Configuration
      SERVICE_ENV_SUFFIX: ${SERVICE_ENV_SUFFIX}
      SERVICE_HOST: ${SERVICE_HOST}
      # System Configuration
      SYSTEM_NOTIFICATION_EMAIL: ${SYSTEM_NOTIFICATION_EMAIL}
      # Service Ports (all services need to know all ports for inter-service communication)
      PATIENT_SERVICE_PORT: ${PATIENT_SERVICE_PORT}
      THERAPIST_SERVICE_PORT: ${THERAPIST_SERVICE_PORT}
      MATCHING_SERVICE_PORT: ${MATCHING_SERVICE_PORT}
      COMMUNICATION_SERVICE_PORT: ${COMMUNICATION_SERVICE_PORT}
      GEOCODING_SERVICE_PORT: ${GEOCODING_SERVICE_PORT}
      SCRAPING_SERVICE_PORT: ${SCRAPING_SERVICE_PORT}
      # Internal Service Ports (for container-to-container communication)
      PATIENT_SERVICE_INTERNAL_PORT: ${PATIENT_SERVICE_INTERNAL_PORT}
      THERAPIST_SERVICE_INTERNAL_PORT: ${THERAPIST_SERVICE_INTERNAL_PORT}
      MATCHING_SERVICE_INTERNAL_PORT: ${MATCHING_SERVICE_INTERNAL_PORT}
      COMMUNICATION_SERVICE_INTERNAL_PORT: ${COMMUNICATION_SERVICE_INTERNAL_PORT}
      GEOCODING_SERVICE_INTERNAL_PORT: ${GEOCODING_SERVICE_INTERNAL_PORT}
      # Matching Configuration
      MAX_ANFRAGE_SIZE: ${MAX_ANFRAGE_SIZE}
      MIN_ANFRAGE_SIZE: ${MIN_ANFRAGE_SIZE}
      PLZ_MATCH_DIGITS: ${PLZ_MATCH_DIGITS}
      DEFAULT_MAX_DISTANCE_KM: ${DEFAULT_MAX_DISTANCE_KM}
      # Follow-up Configuration
      FOLLOW_UP_THRESHOLD_DAYS: ${FOLLOW_UP_THRESHOLD_DAYS}
      DEFAULT_PHONE_CALL_TIME: ${DEFAULT_PHONE_CALL_TIME}
    ports:
      - "${MATCHING_SERVICE_PORT}:${MATCHING_SERVICE_INTERNAL_PORT}"
    depends_on:
      pgbouncer-prod:
        condition: service_healthy
      kafka-prod:
        condition: service_healthy
    command: python app.py
    networks:
      - curavani_backend_prod

  # Communication Service
  communication_service-prod:
    build:
      context: .
      dockerfile: communication_service/Dockerfile
    container_name: communication_service-prod
    environment:
      # Core Database Configuration
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_HOST: postgres-prod
      DB_PORT: ${DB_PORT}
      # PgBouncer Configuration
      PGBOUNCER_HOST: pgbouncer-prod
      PGBOUNCER_PORT: ${PGBOUNCER_PORT}
      # Kafka Configuration
      KAFKA_BOOTSTRAP_SERVERS: ${KAFKA_BOOTSTRAP_SERVERS}
      KAFKA_LOG_LEVEL: ${KAFKA_LOG_LEVEL}
      # Flask Configuration
      FLASK_ENV: ${FLASK_ENV}
      FLASK_DEBUG: ${FLASK_DEBUG}
      LOG_LEVEL: ${LOG_LEVEL}
      # Security Configuration
      SECRET_KEY: ${SECRET_KEY}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      # Service Configuration
      SERVICE_ENV_SUFFIX: ${SERVICE_ENV_SUFFIX}
      SERVICE_HOST: ${SERVICE_HOST}
      # Service Ports (all services need to know all ports for inter-service communication)
      PATIENT_SERVICE_PORT: ${PATIENT_SERVICE_PORT}
      THERAPIST_SERVICE_PORT: ${THERAPIST_SERVICE_PORT}
      MATCHING_SERVICE_PORT: ${MATCHING_SERVICE_PORT}
      COMMUNICATION_SERVICE_PORT: ${COMMUNICATION_SERVICE_PORT}
      GEOCODING_SERVICE_PORT: ${GEOCODING_SERVICE_PORT}
      SCRAPING_SERVICE_PORT: ${SCRAPING_SERVICE_PORT}
      # Internal Service Ports (for container-to-container communication)
      PATIENT_SERVICE_INTERNAL_PORT: ${PATIENT_SERVICE_INTERNAL_PORT}
      THERAPIST_SERVICE_INTERNAL_PORT: ${THERAPIST_SERVICE_INTERNAL_PORT}
      MATCHING_SERVICE_INTERNAL_PORT: ${MATCHING_SERVICE_INTERNAL_PORT}
      COMMUNICATION_SERVICE_INTERNAL_PORT: ${COMMUNICATION_SERVICE_INTERNAL_PORT}
      GEOCODING_SERVICE_INTERNAL_PORT: ${GEOCODING_SERVICE_INTERNAL_PORT}
      # SMTP Configuration
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USERNAME: ${SMTP_USERNAME}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      SMTP_USE_TLS: ${SMTP_USE_TLS}
      EMAIL_SENDER: ${EMAIL_SENDER}
      EMAIL_SENDER_NAME: ${EMAIL_SENDER_NAME}
      EMAIL_ADD_LEGAL_FOOTER: ${EMAIL_ADD_LEGAL_FOOTER}
      # Company Information
      COMPANY_NAME: ${COMPANY_NAME}
      COMPANY_STREET: ${COMPANY_STREET}
      COMPANY_PLZ: ${COMPANY_PLZ}
      COMPANY_CITY: ${COMPANY_CITY}
      COMPANY_COUNTRY: ${COMPANY_COUNTRY}
      COMPANY_CEO: ${COMPANY_CEO}
      COMPANY_HRB: ${COMPANY_HRB}
      LEGAL_FOOTER_PRIVACY_TEXT: ${LEGAL_FOOTER_PRIVACY_TEXT}
      # System Configuration
      SYSTEM_NOTIFICATION_EMAIL: ${SYSTEM_NOTIFICATION_EMAIL}
      # Communication Configuration
      COMMUNICATION_EMAIL_BATCH_LIMIT: ${COMMUNICATION_EMAIL_BATCH_LIMIT}
      COMMUNICATION_TIMEOUT_SHORT: ${COMMUNICATION_TIMEOUT_SHORT}
      COMMUNICATION_TIMEOUT_LONG: ${COMMUNICATION_TIMEOUT_LONG}
      # Follow-up Configuration
      DEFAULT_PHONE_CALL_TIME: ${DEFAULT_PHONE_CALL_TIME}
    ports:
      - "${COMMUNICATION_SERVICE_PORT}:${COMMUNICATION_SERVICE_INTERNAL_PORT}"
    depends_on:
      pgbouncer-prod:
        condition: service_healthy
      kafka-prod:
        condition: service_healthy
    command: python app.py
    networks:
      - curavani_backend_prod

  # Geocoding Service
  geocoding_service-prod:
    build:
      context: .
      dockerfile: geocoding_service/Dockerfile
    container_name: geocoding_service-prod
    environment:
      # Core Database Configuration
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_HOST: postgres-prod
      DB_PORT: ${DB_PORT}
      # PgBouncer Configuration
      PGBOUNCER_HOST: pgbouncer-prod
      PGBOUNCER_PORT: ${PGBOUNCER_PORT}
      # Kafka Configuration
      KAFKA_BOOTSTRAP_SERVERS: ${KAFKA_BOOTSTRAP_SERVERS}
      KAFKA_LOG_LEVEL: ${KAFKA_LOG_LEVEL}
      # Flask Configuration
      FLASK_ENV: ${FLASK_ENV}
      FLASK_DEBUG: ${FLASK_DEBUG}
      LOG_LEVEL: ${LOG_LEVEL}
      # Security Configuration
      SECRET_KEY: ${SECRET_KEY}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      # Service Configuration
      SERVICE_ENV_SUFFIX: ${SERVICE_ENV_SUFFIX}
      SERVICE_HOST: ${SERVICE_HOST}
      # System Configuration
      SYSTEM_NOTIFICATION_EMAIL: ${SYSTEM_NOTIFICATION_EMAIL}
      # Service Ports (all services need to know all ports for inter-service communication)
      PATIENT_SERVICE_PORT: ${PATIENT_SERVICE_PORT}
      THERAPIST_SERVICE_PORT: ${THERAPIST_SERVICE_PORT}
      MATCHING_SERVICE_PORT: ${MATCHING_SERVICE_PORT}
      COMMUNICATION_SERVICE_PORT: ${COMMUNICATION_SERVICE_PORT}
      GEOCODING_SERVICE_PORT: ${GEOCODING_SERVICE_PORT}
      SCRAPING_SERVICE_PORT: ${SCRAPING_SERVICE_PORT}
      # Internal Service Ports (for container-to-container communication)
      PATIENT_SERVICE_INTERNAL_PORT: ${PATIENT_SERVICE_INTERNAL_PORT}
      THERAPIST_SERVICE_INTERNAL_PORT: ${THERAPIST_SERVICE_INTERNAL_PORT}
      MATCHING_SERVICE_INTERNAL_PORT: ${MATCHING_SERVICE_INTERNAL_PORT}
      COMMUNICATION_SERVICE_INTERNAL_PORT: ${COMMUNICATION_SERVICE_INTERNAL_PORT}
      GEOCODING_SERVICE_INTERNAL_PORT: ${GEOCODING_SERVICE_INTERNAL_PORT}
      # Geocoding Configuration
      OSM_API_URL: ${OSM_API_URL}
      OSM_USER_AGENT: ${OSM_USER_AGENT}
      OSM_TIMEOUT: ${OSM_TIMEOUT}
      OSM_MAX_RETRIES: ${OSM_MAX_RETRIES}
      OSM_RATE_LIMIT: ${OSM_RATE_LIMIT}
      OSRM_API_URL: ${OSRM_API_URL}
      OSRM_PROFILE_CAR: ${OSRM_PROFILE_CAR}
      OSRM_PROFILE_TRANSIT: ${OSRM_PROFILE_TRANSIT}
      # Cache Configuration
      CACHE_TTL_SECONDS: ${CACHE_TTL_SECONDS}
      CACHE_MAX_SIZE: ${CACHE_MAX_SIZE}
      GEOCODING_REQUEST_TIMEOUT: ${GEOCODING_REQUEST_TIMEOUT}
      GEOCODING_RETRY_DELAY_BASE: ${GEOCODING_RETRY_DELAY_BASE}
      GEOCODING_RATE_LIMIT_SLEEP: ${GEOCODING_RATE_LIMIT_SLEEP}
      GEOCODING_COORDINATE_PRECISION: ${GEOCODING_COORDINATE_PRECISION}
    ports:
      - "${GEOCODING_SERVICE_PORT}:${GEOCODING_SERVICE_INTERNAL_PORT}"
    volumes:
      - ./data:/data:ro  # Mount data folder as read-only
    depends_on:
      pgbouncer-prod:
        condition: service_healthy
      kafka-prod:
        condition: service_healthy
    command: python app.py
    networks:
      - curavani_backend_prod

volumes:
  postgres_prod_data:

networks:
  curavani_backend_prod:
    name: curavani_backend_prod
    driver: bridge